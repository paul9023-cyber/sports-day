# ☁ 클라우드 배포 가이드 (PC 없이 대회 진행)

**소요 시간**: 약 15분
**필요한 것**: 이메일 주소 하나
**비용**: 무료
**데이터**: **영구 저장됨** (휘발 X)

배포하면 `https://체육대회-xxxx.onrender.com` 같은 **영구 URL**이 생기고,
패드·휴대폰에서 그 주소만 열면 바로 대회 진행이 가능합니다. **PC는 완전히 꺼도 됩니다.**

---

## 🏗 전체 구조 요약

| 서비스 | 역할 | 비용 |
|--------|------|------|
| **GitHub** | 코드 저장소 | 무료 |
| **Render** | 서버 실행 (웹 호스팅) | 무료 |
| **Upstash Redis** | 데이터 영구 보관 | 무료 |

세 서비스 모두 신용카드 등록 없이 이메일만으로 가입 가능합니다.

---

## Step 1 — GitHub 계정 만들기 (이미 있으면 건너뛰기)

1. https://github.com/signup 접속
2. 이메일 · 비밀번호 · 사용자명 입력
3. 이메일 인증

## Step 2 — 코드를 GitHub에 올리기

1. https://github.com/new 접속
2. Repository name에 `sports-day` 입력
3. **"Public"** 선택 (무료 배포는 공개 저장소만)
4. **"Add a README file"** 체크
5. 초록색 **"Create repository"** 클릭
6. 만들어진 저장소 화면에서 **"Add file"** → **"Upload files"** 클릭
7. 이 폴더(`04. 운동회 프로그램`) **안의 파일 전체**를 드래그
   - `cloudflared.exe`와 `data.json`은 자동 제외됨 (`.gitignore` 덕분)
8. 아래 **"Commit changes"** 클릭

## Step 3 — Upstash Redis DB 만들기 (🔑 데이터 영구 저장소)

**이게 빠지면 데이터가 서버 재시작 시 사라집니다. 꼭 하세요.**

1. https://console.upstash.com/login 접속
2. **"Google"** 또는 **"GitHub"** 로 가입 (1클릭)
3. 대시보드에서 **"Create Database"** 클릭
4. 설정 입력:

   | 항목 | 값 |
   |------|-----|
   | **Name** | `sports-day` |
   | **Type** | **Regional** |
   | **Region** | **`ap-northeast-1 (Tokyo)`** (한국에서 가장 가까움) |
   | **TLS** | 켜짐 (기본값) |
   | **Eviction** | 꺼짐 (기본값) |

5. **"Create"** 클릭
6. DB가 만들어지면 **"REST API"** 섹션으로 내려가 두 값 복사:
   - `UPSTASH_REDIS_REST_URL` (예: `https://xxx-xxx-12345.upstash.io`)
   - `UPSTASH_REDIS_REST_TOKEN` (긴 문자열)

   → **메모장에 임시로 붙여넣어 두세요. Step 4에서 씁니다.**

## Step 4 — Render.com에 배포

1. https://render.com/register 접속
2. **"GitHub"** 로 가입 (1클릭)
3. GitHub 연동 권한 **"Authorize"**
4. Render 대시보드에서 **"New +"** → **"Web Service"** 클릭
5. 앞서 만든 `sports-day` 저장소 선택 → **"Connect"**
6. 설정 화면에서 다음을 확인·입력:

   | 항목 | 값 |
   |------|-----|
   | **Name** | `sports-day` (이게 URL에 들어감) |
   | **Region** | `Singapore` |
   | **Branch** | `main` |
   | **Runtime** | `Python 3` |
   | **Build Command** | (비워두기) |
   | **Start Command** | `python app.py` |
   | **Instance Type** | **`Free`** |

7. 아래 **"Environment Variables"** 섹션 펼치기 → **"Add Environment Variable"** 두 번 클릭해 추가:

   | Key | Value |
   |-----|-------|
   | `UPSTASH_REDIS_REST_URL` | (Step 3에서 복사한 URL) |
   | `UPSTASH_REDIS_REST_TOKEN` | (Step 3에서 복사한 토큰) |

8. 맨 아래 **"Create Web Service"** 클릭
9. **2~3분 기다림**. 로그 창에 이런 줄이 뜨면 성공:
   ```
   [cloud] Server listening on 0.0.0.0:10000
   [cloud] 영구 저장: Upstash Redis (key=sportsday:state)
   ```
10. 상단에 `https://sports-day-xxxx.onrender.com` 주소가 뜸 — 이게 **대회용 영구 URL**

## Step 5 — (권장) 서버 잠자기 방지 설정 (2분)

Render 무료 플랜은 15분 이상 접속 없으면 잠자기 모드가 되어, 다음 접속 시 30~50초 대기가 발생합니다. 이걸 방지하려면:

1. https://uptimerobot.com 가입 (무료)
2. **"Add New Monitor"** 클릭
3. Monitor Type: **HTTP(s)**
4. URL: Render URL + `/api/state` (예: `https://sports-day-xxxx.onrender.com/api/state`)
5. Monitoring Interval: **5 minutes**
6. **"Create Monitor"**

이렇게 하면 5분마다 자동 핑 → 서버가 절대 잠들지 않음.
(데이터는 어차피 Upstash에 저장되므로 잠들었다 깨어나도 안전하지만, 초기 접속 지연을 없애주는 용도입니다.)

---

## Step 6 — 대회 당일

1. **패드에서** Render URL 접속 → **"본부"** 선택 → 학교명·학년·종목 설정
2. 본부 화면 상단 **"📱 심판 초대"** 버튼 → QR 코드 표시
3. 심판 18명이 각자 휴대폰 카메라로 QR 스캔 → **"심판"** 선택 → 반 선택
4. 끝날 때까지 **패드·휴대폰만** 있으면 됨

---

## 🔄 코드 업데이트하기

나중에 코드를 수정하고 싶으면:

1. GitHub 저장소에 파일 업로드 (기존 파일 덮어쓰기)
2. Render가 **자동으로 감지해서 재배포**
3. 2~3분 후 새 버전 적용
4. **데이터는 그대로 유지됨** (Upstash에 있으니까)

---

## 💾 데이터 관리

| 필요한 일 | 방법 |
|-----------|------|
| 대회 후 점수 데이터 보관 | Render URL의 `/api/state` 접속 → JSON 전체가 화면에 뜸 → 복사해서 저장 |
| 다음 대회를 위해 초기화 | 본부 화면 → 설정 탭 → **"전체 초기화"** 버튼 |
| DB 크기 확인 | https://console.upstash.com 대시보드 (대회 6시간 사용해도 1MB 미만) |

---

## 문제 해결

| 상황 | 해결 |
|------|------|
| Render 배포 실패 | Render 대시보드 "Logs" 탭에서 오류 확인. 대부분 `app.py`가 저장소 루트에 없어서 발생 |
| 로그에 "UPSTASH 환경변수가 없습니다" 경고 | Render → Environment 탭에서 `UPSTASH_REDIS_REST_URL`과 `UPSTASH_REDIS_REST_TOKEN`이 등록됐는지 확인. 수정 후 "Manual Deploy" 클릭 |
| "Application failed to respond" | 서버가 깨어나는 중. 20~50초 후 새로고침 |
| URL 접속되는데 화면 안 뜸 | 브라우저 캐시 지우기 (Ctrl+Shift+R) |
| 데이터가 사라진 것 같음 | 실수로 **"전체 초기화"** 버튼 눌렀을 가능성 큼. Upstash 자체는 데이터를 잃지 않음 |
| Upstash 명령어 초과 (10K/일) | 거의 불가능하나 발생 시 몇 시간 기다리면 리셋됨. 대회 규모로는 1000회 미만 쓰임 |

도움이 필요하면 언제든 물어보세요.

---

## ⚙ 로컬 PC 모드와의 차이

이 프로젝트는 **클라우드 / 로컬 두 모드 자동 전환**됩니다:

| | 로컬 PC (`run.bat`) | 클라우드 (Render) |
|---|---|---|
| 서버 위치 | 본인 PC | Render 서버 |
| 접속 방법 | Wi-Fi 또는 Cloudflare 터널 | 영구 HTTPS URL |
| 데이터 저장 | `data.json` 파일 | Upstash Redis |
| PC 필요 여부 | 필요 (대회 동안 켜둠) | 불필요 |

클라우드 배포 후에도 언제든 `run.bat`으로 로컬 모드로 돌아갈 수 있습니다.
